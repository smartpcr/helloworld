global:
    subscriptionName: {{ .Values.subscriptionName }}
    tenantId: {{ .Values.tenantId }}
    location: {{ .Values.location }}
    dns: {{ .Values.productName }}-{{ .Values.envName }}
    servicePrincipal: {{ .Values.productName }}-{{ .Values.envName }}-{{ .Values.rand }}-{{ .Values.loc }}-sp
    resourceGroup: {{ .Values.productName }}-{{ .Values.envName }}-{{ .Values.rand }}-{{ .Values.loc }}-rg
    keyVault: {{ .Values.productShortName }}-{{ .Values.envName }}-{{ .Values.rand }}-kv
    serviceFabric: {{ .Values.productName }}-{{ .Values.envName }}-{{ .Values.rand }}-{{ .Values.loc }}-sf
    kubernetes: {{ .Values.productName }}-{{ .Values.envName }}-{{ .Values.rand }}-k8s
keyVault:
    certs:
        servicePrincipleCert: {{ .Values.productName }}-{{ .Values.envName }}-{{ .Values.rand }}-sp
        apiSslCert: 
        sfClusterMgrCert:
sfCluster:
    nodeTypes:
        - frontend
        - backend
        - management
aksCluster:
    nodeCount: 3